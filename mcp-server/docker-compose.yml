version: '3.8'

services:
  mcp-server:
    build: .
    container_name: propfirms-mcp-server
    restart: unless-stopped
    expose:
      - "3042"
    environment:
      - SERVICE_FQDN_MCP_SERVER=https://mcp.propfirmstech.com
      - NODE_ENV=production
      - TICKETING_BASE_URL=${TICKETING_BASE_URL}
      - TICKETING_API_KEY=${TICKETING_API_KEY}
      - MCP_SERVER_PORT=3042
      - MCP_SERVER_API_KEY=${MCP_SERVER_API_KEY}
      - DEBUG=false
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3042/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
