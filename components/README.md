# Components Directory

This directory contains shared UI components for the PropFirmsTech Support Portal.

## Structure

- `ui/` - Base UI components (buttons, inputs, badges, etc.)
- `forms/` - Form components and validation
- `layouts/` - Layout components for admin and portal areas
- `tickets/` - Ticket-related components
- `comments/` - Comment-related components
